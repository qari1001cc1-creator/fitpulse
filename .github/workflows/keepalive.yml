name: Keepalive

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render app to keep it awake
        run: curl -fsS https://fitpulse.onrender.com/ || true