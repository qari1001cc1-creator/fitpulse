name: Keepalive

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping FitPulse server to keep it awake
        run: curl -fsS https://fitpulse-fitness.duckdns.org/ || true